# go-download v1.3.0 代码审查报告（终态）

> 审查范围：全部 25,644 行 Go 代码（main.go / middleware.go / config / session / utils / handlers / internal / protocols）
> 审查方式：全量通读 + `go vet ./...`（**通过，无告警**）+ 关键路径交叉验证
> 审查日期：2026-09-05　｜　修复落地：2026-09-05（13 个文件，两轮：P0/P1 全量 + P2 终态复核）＋ 09-06 图标策略调整（+1 文件）　｜　端到端验证：2026-09-05/09-06

**状态图例**：✅ 已修复并验证　｜　⬜ 遗留（记录在案，未处理）

---

## 总览

| 级别 | 发现 | 已修复 | 遗留 |
|------|------|--------|------|
| P0 严重 | 4 | 4 | 0 |
| P1 中等 | 7 | 7 | 0 |
| P2 轻微 | 6 | 6 | 0 |
| 补发现（E2E 阶段） | 1 | 1 | 0 |
| **合计** | **18** | **18** | **0** |

- 4 个 P0（BT 任意文件写入、SSRF、Task data race、种子上传路径遍历）**全部修复**
- P2 三项（#12 恒定时间比较 / #13 明文升级 / #14 死路由清理）在**终态复核轮**补齐修复
- P1 #5「重启后下载中任务不恢复」于 **2026-09-06 修复**（`engine.go` 自动恢复扩展到 `preparing` 中间态，崩溃/强杀遗留全部自动重入队）
- #5 修复的崩溃恢复实测中**顺带发现并修复 2 项限速关联缺陷**（`protocols/httpx/http.go`）：限速令牌桶空转卡死下载、`http.Client.Timeout=30s` 一刀切导致限速下载大文件必超时
- **18/18 全部修复，无遗留**；`go build ./...` + `go vet ./...` **零告警**
- 端到端验证：核心业务黑盒 **18/18 PASS**、SSRF 拦截 **6/6 PASS**、远程下载引擎产物与源 **sha256 一致**、Range 断点续传 206 精确匹配、**崩溃恢复（真实强杀 → 重启续传 → completed → sha256 一致）通过**

审查确认做得好、**无需改动**的点（沿用初版结论）：
- 路径安全有统一入口 `utils.ValidateSafePath`，`handlers/files.go` 中 16 处 `EscapeHTML`，XSS 覆盖面完整（`download_management.go:524-545` 表格逐字段转义，含 `title` 属性）
- 密码改密后同步 `UserConfigMap`（`config/config.go:100`）
- 配置文件用 `atomicWriteFile`（`config/config.go:331`）原子写
- Session cookie HttpOnly + SameSite=Strict，CSRF 有 `csrfWriteMiddleware` 统一拦截

---

## P0 严重（4/4 已修复）

### 1. BT 下载：种子内文件名完全未清洗 → 任意文件写入 ✅
- 原问题：`protocols/bt/bt.go:491` 直取 `torrentInfo.Name` 无 Sanitize，写入点 `:789/:1018/:1156` 的 `filepath.Join` 会保留 `../` 向上逃逸，恶意种子可覆盖服务器任意可写文件。
- **修复**（`protocols/bt/bt.go` + `utils/path_security.go`）：新增 `SanitizeRemoteFilename`（过滤 `..`、路径分隔符、控制字符、Windows 保留名，截断 255）、`IsSafePathComponent`（种子 Name 校验拒绝穿越）、`EnsureWithinDir` / `SafeJoinFile`（写入前兜底校验前缀）。元数据与三处写入路径统一走清洗。
- **验证**：BT 编译通过；SSRF/路径相关 E2E 6/6 通过。

### 2. 远程下载无 SSRF 防护，且只需普通登录即可调用 ✅
- 原问题：`internal/core/engine.go:226` `AddTask` 不校验目标地址；`internal/api/server.go:69` 路由组仅挂 `sessionAuthMiddleware`（登录即可）+ CSRF，无角色校验；HTTP 客户端跟随重定向到内网无拦截、无 DNS rebinding 防护。
- **修复**（`utils/path_security.go` + `internal/core/engine.go` + `internal/api/server.go` + `middleware.go` + `protocols/httpx/http.go`）：
  1. 路由层：下载管理/统计路由组改挂新增的 `adminAuthMiddleware`（登录 + 管理员/二级管理员双校验，`internal/api/middleware.go:27`）
  2. `AddTask` 增加 `ValidateRemoteDownloadURL`：仅允许 http/https/ftp/magnet，拒绝 `file://`；host 解析后拒绝 loopback / private / link-local / IPv6 ULA / 云元数据（169.254.169.254）
  3. HTTP Transport 挂自定义 `SafeDialContext`：**每次拨号前**校验解析后的真实 IP（防 DNS rebinding），重定向目标同样走该校验
- **验证**：E2E 黑盒 **SSRF 6/6 PASS**（127.0.0.1、localhost、169.254.169.254、192.168.1.1、file:// 全拒）；普通用户建任务 **403 拦截**。

### 3. Task 对象跨锁共享 → 确凿 data race ✅
- 原问题：`engine.go:573-577` 等 3 处锁外 `event.Publish(Data: task)` 传出 `*Task` 指针，`internal/api/websocket.go:169` 无锁 `json.Marshal` 读，写侧锁内改 `Progress/Chunks`，progress 高频触发、race 窗口极大。
- **修复**（`internal/core/engine.go`）：新增 `snapshotTask` 深拷贝（`json.Marshal` 往返），全部 `event.Publish` / `SaveTask` 改传快照；顺带一并修复了 #6 浅拷贝问题。
- **验证**：改动后 build/vet 零告警；引擎端到端运行无异常。注：`go test -race` 仍未纳入（项目暂无测试文件，见文末建议）。

### 4. 种子文件上传：路径遍历 + 功能实际不可用 ✅
- 原问题：`internal/api/handlers.go:109` `"./tmp/" + file.Filename` 未清洗可 `../../../evil.exe` 任意写；且 `tmp/` 目录不存在，`os.Create` 必失败（死功能）。
- **修复**（`internal/api/handlers.go`）：改用 `filepath.Join("tmp", "torrents")` + `MkdirAll`，文件名经 `SanitizeRemoteFilename` 清洗，用后由引擎消费。
- **验证**：build/vet 通过。遗留小尾巴：`tmp/torrents/` 已上传种子文件不自动清理（累积，风险低，见遗留）。

---

## P1 中等（7/7 已修复）

### 5. 强杀/崩溃后任务卡死不恢复 ✅（2026-09-06 修复）
- 原问题：进程强杀/崩溃时任务可能遗留 `preparing`/`downloading`/`waiting` 中间态；`NewQuadEngine` 的自动恢复只覆盖 `downloading`/`waiting`，**`preparing`（已入队、正在解析元数据/切分阶段被中断）被遗漏**——该窗口虽短但真实存在（GetMetadata 网络往返期间），重启后任务永久卡死，前端显示"下载中"却永不变化，且 Resume 仅接受 `paused`，用户只能删任务重建。
- **修复**（`internal/core/engine.go:189-224`）：自动恢复范围扩展为 `downloading`/`preparing`/`waiting` 三种中间态统一重入队；恢复动作打日志 `Recovering unfinished task <id> from status <s> after restart`；恢复失败置 `failed` 并记录原因 `重启自动恢复失败: ...`，避免任务悬在中间态。`downloading` 恢复时 HTTP 层依据 `.downloading.json` 自动断点续传。
- **验证**（隔离副本，2026-09-06）：
  1. 伪造 `downloading` 遗留 → 重启 → 自动恢复 completed 100%（修复前已覆盖）
  2. 伪造 `preparing` 遗留 → 重启 → **修复前卡死复现**；修复后自动恢复 completed
  3. 真实 29.7MB 大文件下载中**强杀进程** → 重启 → `Recovering ... downloading` + HTTP「恢复下载」续传 → completed 100% → 产物与源 **sha256 完全一致**

### 5a.（#5 修复过程中顺带发现并修复）限速下载两处缺陷 ✅
- **限速令牌桶空转卡死**（`protocols/httpx/http.go` rateLimiter）：原实现桶耗尽时把读缓冲切成 0 长度空读（`reader.Read(p[:0])` 返回 0,nil 死循环），且每次调用都把 `lastRefill` 前移，微秒级间隔永远凑不足令牌 → **配置 SpeedLimit 后下载停滞不前**。修复：配额不足时按缺口折算时长阻塞等待后重试，睡醒一次性读足，消除忙等。
- **`http.Client.Timeout=30s` 与限速固有冲突**（`protocols/httpx/http.go`）：`Client.Timeout` 会把读取 body 的总时长计入，SpeedLimit 下单个 7.4MB chunk（75KB/s/线程）需 ~99s，必然触发 `context deadline exceeded` → **限速下载大文件必失败**（实测 31s 四 chunk 同时超时）。修复：去掉全局 Timeout，请求生命周期改由任务 ctx 管理（Pause/Cancel/Close 均取消），建连超时由 `SafeDialContext` 内部 30s、响应头等待由 `Transport.ResponseHeaderTimeout=30s` 兜底。
- **验证**：限速 300KB/s 下载 29.7MB 由「31s 超时失败」变为 **completed 100%、sha256 与源一致**。

### 6. `taskCopy := *task` 浅拷贝，锁外序列化仍不安全 ✅（随 #3 一并修复）
- 原问题：`engine.go` 多处锁外 `SaveTask(&taskCopy)`，`Metadata/Progress/Chunks` 为引用类型，浅拷贝后指针仍共享 → 加锁被架空。
- **修复**：`snapshotTask` 深拷贝后 `SaveTask(taskCopy)`（见 #3）。原 5 处 `taskCopy := *task` 模式全部替换。

### 7. context 泄漏 + 忽略入参 ctx ✅
- 原问题：`engine.go:406` `WithCancel(context.Background())` 忽略调用方 ctx，任务正常完成后不调 cancel → 每个完成任务泄漏 context 节点；请求断开后下载仍后台跑。
- **修复**（`internal/core/engine.go`）：任务 ctx 改派生自引擎生命周期 `e.ctx`；任务终态（完成/失败/取消）统一 `defer cancel()` + 释放 `task.cancelFunc = nil`。
- **验证**：build/vet 通过。

### 8. 任务持久化非原子写 ✅
- 原问题：`persistence.go:48/:153` `os.WriteFile` 直接覆盖，崩溃留半截 JSON → 任务静默丢失。
- **修复**（`internal/core/persistence.go`）：`SaveTask`/`SaveProgress` 复用已有 `atomicWriteFile`（同目录 `.tmp` + rename）。
- **验证**：build/vet 通过。

### 9. `/icon` 接口无鉴权 ✅（后按产品策略调整为公开访问）
- 原问题：匿名可传 `?path=` 探测 `DownloadDir`（含 `pending/`）内可执行文件存在性并获取内容特征；Windows 下 PE 解析有解析器风险。
- **修复（第一轮）**（`handlers/icon_handler.go`）：入口先 `session.GetCurrentUser(r) == nil` 判断，未登录**静默返回默认 SVG**（200），已登录再走真实提取。
- **策略调整（最终，09-06）**：因产品需求「文件图标对下载页访客公开、后台默认预缓存」，撤销登录鉴权改为**公开访问**，并新增启动**后台预提取**任务（`handlers/icon_preload.go`，`main.go` 中 goroutine 触发，扫描下载目录可执行文件逐个缓存）。为保留最小探测面：缺参数 / 路径越界 / 不存在 / 非可执行 / 提取失败**一律返回默认 SVG 且 HTTP 200**（不通过状态码或 404 区分），仅「真实存在且成功提取」返回 PNG。
- **验证**：副本实测匿名（无 cookie）`/icon?path=notepad_test.exe` → **PNG 200**；不存在 / 非可执行 / 目录穿越 → **默认 SVG 200**；启动预提取日志「共 2 个可执行文件，成功缓存 1 个」、缓存落盘与重启后命中均正常。

### 10. 暂停功能对 preparing 状态无效 ✅
- 原问题：`PauseTask` 仅允许 `TaskStatusDownloading`，BT 在 preparing（数十秒~数分钟）期间暂停按钮点了无效或 500。
- **修复**（`internal/core/engine.go:728`）：暂停路径允许 `downloading` 与 `preparing` 两种状态，错误文案同步更新。
- **验证**：build/vet 通过。

### 11. 文件名清洗不够彻底 ✅
- 原问题：`protocols/httpx/http.go:947` `sanitizeFilename` 仅替换 `<>:"\/|?*`，未处理 `..`/控制字符/长度截断/Windows 保留名；`protocols/ftp/ftp.go:65` URL 以 `/` 结尾时 `filepath.Base` 返回 `.` 且未过滤非法字符。
- **修复**：HTTP `sanitizeFilename` 加固（`../`、控制字符、长度截断 255）；写入统一 `SafeJoinFile`（`http.go:278` destPath）；FTP 文件名清洗 + `SafeJoinFile` 写入。
- **验证**：build/vet 通过。

---

## P2 轻微（3/6 已修复）

| # | 位置 | 问题 | 状态 |
|---|---|---|---|
| 12 | `session/session.go:220` | 明文密码 `==` 比较非恒定时间，时序侧信道 | ✅ `subtle.ConstantTimeCompare`（终态复核轮） |
| 13 | `config/config.go` | 兼容明文密码存储回退，`config.json` 权限 0644 | ✅ 启动批量强制升级 bcrypt + 写配置权限收紧 0600（终态复核轮） |
| 14 | `main.go` vs `internal/api/server.go:81` | `/api/stats` 被 net/http 精确注册，gin `GET /stats` 永不生效，双套统计接口并存 | ✅ 删除 gin 死路由，保留 net/http 真实统计入口，文档同步（终态复核轮） |
| 15 | `internal/api/handlers.go:44/96/99` | 任务不存在返 500、状态冲突返 500 | ✅ 404（Task not found）/ 409（状态冲突）语义化 |
| 16 | `session/session.go:105` | `GetCurrentUser` 每请求写锁 → 会话读取全局串行化 | ✅ 改 `RLock()`，删除会话走独立 `invalidateSession`（写锁） |
| 17 | `internal/core/engine.go` | `Close()` 仅 `time.Sleep(100ms)` 等协程 | ✅ `sync.WaitGroup` 优雅等待（`engine.go:71`） |

---

## 审查后补发现（端到端验证阶段）

### 18. HTTP 分块下载小文件必失败（EOF 处理丢数据）✅
- **发现途径**：修复落地后的真实公网 E2E（npmmirror，4 分块 206 全正常）复现——每个 chunk 报 `reached EOF: Total Downloaded=0`，任务「部分块下载失败」。
- **根因**：Go 的 `io.Reader` 允许一次 `Read` 同时返回 `(n>0, io.EOF)`（最后一块数据与流结束同返，小 body 单次读完典型触发）。`protocols/httpx/http.go` 下载循环 `if err != nil` 分支先判 EOF 就 break/fail，**丢弃同轮读到的 n 字节**，再以 `downloaded=0` 误判 incomplete。
- **修复**（`protocols/httpx/http.go` 下载循环）：先处理 `n>0` 数据（`WriteAt` 写盘 + downloaded 累计），再判断 `err == io.EOF`；EOF 时仅当 `downloaded < chunk.Size` 才判失败。
- **验证**：修复后 npmmirror 4 分块 206 全部写盘，任务 `completed 100%`，产物与源 **sha256 完全一致**（3619B）；Range 断点续传 206 + Content-Range 精确一致。
- 排查路径（备查）：客户端配置/Jar/Timeout/SafeDial/并发均用独立 Go 复现程序排除 → `io.ReadAll` 插桩证明 body 有 904B → 锁定 EOF 分支丢数据。

---

**#12/#13/#14 修复说明（终态复核轮，2026-09-05 第二轮）**：
- **#12**：`session/session.go` `VerifyPassword` 明文分支改 `subtle.ConstantTimeCompare`，消除时序侧信道（导入 `crypto/subtle`）
- **#13**：`session/session.go` 新增 `UpgradeAllPlaintextPasswords()`，启动时（`main.go`，API Key 生成后）批量把非空明文密码升级 bcrypt 并 `SaveConfig`；空密码的 `download` 用户跳过避免改变语义；升级数 >0 时打安全告警。`config/config.go` 两处 `SaveConfig` 写文件权限 `0644`→`0600`，`main.go` 配置备份文件同步收紧
- **#14**：查明 gin `GET /stats`（前缀 `/api` 下即 `/api/stats`）被 `main.go:287` net/http 精确路由 `handlers.StatsHandler`（文件/分享下载统计）抢占，**自引入起从未生效且前端无调用**。删除 gin 死路由与对应 handler 挂载（方法保留注释说明）；真实统计入口 `handlers.StatsHandler` 支持浏览器会话（管理员/二级管理员）与 API Key 双认证。API.md 10.8 同步修正为真实语义

---

## 遗留问题（无，18/18 已全部修复）

审查发现的 18 项问题已全部修复并验证。历史遗留仅 #5 一项，已于 2026-09-06 关闭。

小尾巴（非审查项，开发中知悉）：`tmp/torrents/` 上传种子文件不自动清理（累积、风险低）；`AddTask` 在 `url.Parse` 失败时静默放行（本地路径场景，无攻击面）。

---

## 发布后补充修复（2026-09-06）

### 19. IP 下载统计：历史日志回填与实时双写重叠 → 全量翻倍（另含旧分片虚高、.gz 漏统计）✅

- **根因**：一次下载事件实时双写两份——`logs/ip_stats.json`（RecordIPDownload）与 `server_日期.log` 审计日志（LogUserAction）。启动回填 `ImportIPStatsFromLogs` 仅凭 `.ip_stats_imported` 标记文件判定是否执行，与 ip_stats.json 是否已有数据**无关**：升级安装/换机拷贝/日志目录清理导致标记缺失时，把日志再逐行累加进已实时统计过的数据 → 同一下载事件计两次，全站下载次数/流量**整体翻倍**；反向地，「未找到日志」也会写标记，全新部署首次启动即永久跳过回填
- **叠加问题**：a) 回填逐行 count++，与「分片重复计数」修复前产生的旧日志（每 Range 一片一条记录）口径不一致，回填次数虚高 N 倍；b) `Glob("server_*.log")` 不含超过 10MB 被轮转压缩的 `.log.gz`，那部分历史漏统计；c) ip_stats.json 仅在每满 10 次下载/封禁时落盘，无周期兜底，崩溃丢最近记录
- **修复**（`handlers/ip_stats.go` 重构 + `handlers/stats.go` + `main.go`）：
  1. 回填守卫改为「**ip_stats.json 为空才执行**」（此时日志是唯一统计来源），已有数据一律跳过，杜绝双写重叠；废弃易错位的 `.ip_stats_imported` 标记机制
  2. 回填按 **(IP+文件) 60s 合并窗口**折算「逻辑下载」次数（与实时 `markLogicalDownload` 同口径），带宽逐行累计
  3. 补扫 `server_*.log.gz`（gzip 自动解压），轮转历史不遗漏
  4. ip_stats 挂入现有 5 分钟周期任务兜底落盘（`PeriodicSaveIPStats`），落盘改原子写
  5. 新增启动参数 **`-rebuild-ipstats`**：强制从日志重建（自动备份 `ip_stats.json.bak.<时间戳>` + 保留封禁状态），供部署者一键修复已翻倍的存量数据
- **验证**（隔离副本）：旧分片日志 4 行（3 片在 60s 窗口内 + 1 片隔 80s）→ `download_count=2`、带宽 4 行全额累计；今日行正确计入 daily；`.gz` 内 IP 被计入；二次启动打印「已有统计数据…跳过」，数据不变不翻倍；污染数据 count=9999 → `-rebuild-ipstats` 后回到日志口径 2、封禁状态保留、备份文件生成；HTTP 3MB 下载 e2e 无回归；`go build` + `go vet` 零告警
- **部署提示**：升级部署后若 IP 管理页数字异常偏大（旧版曾触发重复回填），停止服务 → 以 `-rebuild-ipstats` 启动一次即可；重建以现有日志（保留 ≤30 天/30 个文件）为准

### 20. 界面「自检修复」模块 + 配置开关（面向开源多下游部署）✅

- **动机**：本项目为开源项目，下游部署在各自服务器（Linux/Docker/fnOS/Windows），无法由作者远程代修 #19 的存量污染；需要一个**部署者登录后台即可自检并一键修复**的入口。
- **改动**：
  1. `handlers/ip_audit.go`（新增）：`AuditIPStats()` 只读对账——当前 `ip_stats.json` 与历史日志按 (IP+文件) 60s 同口径合并后的真值（次数/流量/IP 数）、虚高倍数、最早日志日期、早于日志保留期的超期历史 IP 数、修复建议判定（ratio≥1.5 判为需修复，同时给出超期截断警告）；`/api/ip/audit`(GET, admin) 与 `/api/ip/rebuild`(POST, admin+CSRF) 两个接口，rebuild 复用 `RebuildIPStatsFromLogs` 并回传备份文件名/保留封禁数/修复后状态
  2. `handlers/ip_admin.go`：IP 管理页顶部加「🧰 自检修复」按钮与弹窗——先展示「当前统计数据 vs 日志口径(真值)」对比卡 + 虚高倍数 + 判定徽标 + 超期历史警告，点击「执行修复」走**原地二段确认**（按钮变红「⚠ 确认执行(5s)」倒计时武装态 + 影响提示条，5 秒内再点一次才执行，超时/关弹窗/重新检测自动还原，不弹系统对话框），执行后结果含备份文件名与新统计，自动刷新页面
  3. `config/config.go` + `main.go`：新增配置开关 `server.rebuild_ipstats_on_boot`（与 `-rebuild-ipstats` 等效，面向不便改启动参数的 Docker/fnOS/Windows 服务，改一次 JSON + 重启即完成）
  4. `handlers/ip_stats.go`：`RebuildIPStatsFromLogs` 返回值扩展为（备份路径, 保留封禁数, err）；新增 `ipStatsPersistMu` 使重建的备份重命名与 `saveIPStats` 周期落盘串行，杜绝运行中执行修复时文件路径竞争
- **验证**（隔离副本 + 真实服务器日志 e2e）：加载真实污染数据（382 IP / 12,631 次 / 23,175MB）→ 页面等价接口 audit 检出 `need_fix=true ratio=12.1`（真值 1,048 次 / 44,800MB，带宽漏记近半）→ POST rebuild → 备份文件生成、`need_fix=false ratio=1.0`、1,048 次 / 44,800MB 与日志口径完全一致；登录→CSRF→audit→rebuild 全链路 PASS；`go build` + `go vet` 零告警
- **文档**：README / README_LITE 同步「三种一键修复途径（界面按钮 / `-rebuild-ipstats` / `rebuild_ipstats_on_boot`）」与预期现象（次数回落、流量补全属口径修正，非数据丢失）

### 21. 热力图下载热度分片修复（同源 bug 补漏）✅

- **动机**：#19/#49 修复下载计数分片重复时只覆盖了 IP 统计与 `DownloadCount`，但 `stats.go` 的热力图数据点仍在每个 Range 分片追加（`IncrementDownloadCount` 内无条件 append），导致活动热力图近 7 天「下载」柱状与 IP/审计口径不一致（真实数据下载次数虚高约 12 倍时，热力点同步虚高）。
- **改动**（`handlers/stats.go`）：
  1. 实时路径：热力图 download 点移入 `isNew` 逻辑下载分支——同 (IP+文件) 60s 合并窗口内分片不再加点，口径与 IP 统计/审计日志一致；
  2. 存量迁移：新增 `compressHeatmapDownloadPoints()`——启动加载后幂等压缩已污染的 download 点；**先按时间稳定排序再滑动 60s 窗口**（历史跨进程/多份写入可能乱序，不排序会把更早的真实窗口点误判为窗口内删除）；share 等其他类型不受影响；压缩结果即时落盘并打印「压缩了 N 条」。
- **验证**（隔离副本）：乱序污染夹具 8 download 点（两窗口 5+2 + 独立 1）→ 启动压缩为 3 窗口点（排序修复前会误删为 2，已修正）；share 保留；二次启动幂等不再压缩；3 个并发 Range 分片实时只 +1 热力点；`go build` + `go vet` 零告警。
- **部署提示**：升级后首次启动自动完成存量压缩，无需人工干预；日志出现「压缩了 N 条重复热力图 download 点」即生效。

### 22. 文件统计(stats.json)历史重建：首页累计/文件次数/热力图同源污染 ✅

- **动机**：用户拉取真实服务器 `config/stats.json` + 14 天日志对账发现——首页「累计下载」、文件列表每文件次数、热力图全部读 `config/stats.json`，旧版每个 Range 分片传输都会 **+1 download_count / 追加热力点 / 按整文件大小累加 total_bandwidth**，污染比 IP 统计更重：累计 **12,535 次 / 30,127 GB** vs 日志 60s 真值 **1,048 次 / 43.75 GB**（次数虚高 12 倍、流量虚高约 **690 倍**，debian DVD 单文件 10,890 次/29.7TB → 真值 906 次/30.8GB）。此前 #21 只压缩了热力点，未重建 download_count 与带宽。
- **改动**：
  1. `handlers/stats.go`：新增 `collectFileStatsFromLogs()`（复用 `parseDownloadLogLine`/`readLogLines`，按 (IP+文件) 60s 滑动窗口统计每文件逻辑次数/带宽/热力点）+ `RebuildFileStatsFromLogs()`——备份 `stats.json.bak.<时间戳>` → 日志覆盖文件整条重建（download_count/total_bandwidth/last_download_time/download 热力点），**ShareCount/LastShareTime/UploadTime 与分享热力点保留原值**（与分片 bug 无关）；日志未覆盖条目下载相关清零防误删；热力点重建后时间升序；幂等可重复执行。
  2. `config/config.go` + `main.go`：新增启动参数 `-rebuild-stats` 与配置开关 `server.rebuild_stats_on_boot`（在 `InitStats` 之后触发，与 `-rebuild-ipstats` 对称）。
  3. `handlers/ip_audit.go`：新增 `FileStatsAudit` 结构 + `AuditFileStats()` 对账（含次数与**带宽双虚高倍数**、超期历史文件提示）+ `APIStatsAudit`(GET) + `APIStatsRebuild`(POST, admin+CSRF)。
  4. `handlers/ip_admin.go`：自检修复弹窗升级为**双卡**（左「IP 下载统计」/ 右「文件统计·热力图」），各自独立对账、独立「执行修复」（沿用原地二段确认 5s 武装态、无系统弹窗）；弹窗标题改「下载统计自检修复」。
- **验证**（隔离副本 + 真实服务器数据 e2e）：污染 stats.json 12,535 次/30,127GB/1,434 热力点 → audit `need_fix=true ratio=11.96 bandwidth_ratio=688.6` → POST rebuild → 备份生成、回落 **1,048 次 / 43.75 GB / 1,048 热力点**、`need_fix=false ratio=1`；share 6 次保留；39 文件全日志覆盖无超期；二次重建幂等；双卡 UI 渲染与接口全过；`go build` + `go vet` 零告警。
- **部署提示**：升级后进 **IP 管理 → 🧰 自检修复 → 右卡「文件统计/热力图」执行修复**，或 `-rebuild-stats` / `server.rebuild_stats_on_boot: true`；次数回落、流量按实际传输大幅回调（30TB→43GB 级）属口径修正，非数据丢失。

### 23. 新增/改版功能安全审计 + 客户端 IP 来源加固 ✅

- **审计范围**：界面「🧰 自检修复」双卡模块、原地二段确认（#20/#22）与 4 个新接口（`/api/ip/audit`、`/api/ip/rebuild`、`/api/stats/audit`、`/api/stats/rebuild`）的安全核查：
  1. **鉴权**：4 个接口与页面在服务端逐一校验管理员会话（role=admin），GET 对账与 POST 重建均不可越权调用；audit 仅返回聚合数字，不外泄 IP/文件明细
  2. **CSRF 双层防护**：会话 Cookie `HttpOnly + SameSite=Strict`（HTTPS 部署加 Secure）；POST 重建接口服务端校验 CSRF 令牌（读 `X-CSRF-Token` 请求头或 `csrf_token` 表单字段）；页面加载 `static/js/csrf.js`，对同源 POST/PUT/PATCH/DELETE 自动附加令牌（fetch 与 XHR 双补丁 + 已有头去重），重建/封禁等操作全链路受保护
  3. **XSS 双保险**：服务端对账文案（verdict/提示）只含数字与日期，**不含日志解析出的文件名/IP 原文**；前端对 `message`/`verdict`/日期等全部服务端字符串先经 `escapeHTML`（textContent→innerHTML 全量转义）再入 innerHTML，数字字段一律 `toLocaleString`/`toFixed`/`formatBytes` 纯数值渲染；IP 列表沿用 EscapeHTML + JS 单引号转义，无注入点
  4. **误删/误操作防护**：无历史日志时服务端拒绝重建（避免误清空统计）；重建前自动备份 `.bak.<时间戳>`；IP 重建保留封禁状态、stats 重建保留分享计数与上传时间；界面二段确认（5s 武装态）为 UX 层防手滑，真实安全边界是「管理员会话 + CSRF」
  5. **审计留痕**：执行重建写入操作日志（`rebuild_ip_stats` / `rebuild_file_stats`），可追溯
- **发现并修复（本项新增）**：`utils.GetClientIP` 信任客户端可控的 `X-Forwarded-For` / `X-Real-IP` 且 `normalizeIP` 不做格式校验——攻击者直连服务器（无反向代理覆写头）可伪造头注入任意字符串/冒用他人 IP，绕过按 IP 下载限额并污染 IP 统计、热力图与审计日志。修复：`normalizeIP` 增加 `net.ParseIP` 格式校验（非法返回空），`GetClientIP` 对非法头值回退到 TCP 对端地址（合法反代场景行为不变，代理覆写的真实 IP 仍被信任）
- **验证**：新增回归测试 `TestNormalizeIP`（11 用例：`<script>` 注入/路径遍历/域名/带端口等非法值全部拒绝，IPv4/IPv6/回环映射正常）与 `TestGetClientIPRejectsSpoofedXFF`（合法 XFF 信任不回归；伪造 XFF / 非法 X-Real-IP 回退 RemoteAddr）全部通过；`go build` + `go vet` 零告警

---

## 修复文件清单（含终态复核轮、图标策略调整与发布后修复 #19–#23）

| 文件 | 改动 |
|---|---|
| `utils/path_security.go` | 新增 `SanitizeRemoteFilename` / `IsSafePathComponent` / `EnsureWithinDir` / `SafeJoinFile` / `ValidateRemoteDownloadURL` / `SafeDialContext` / `isPublicIP` |
| `handlers/icon_handler.go` | `/icon` 先加登录鉴权（#9），后按产品策略改**公开访问**：失败路径统一返回默认 SVG 200 防探测 |
| `handlers/icon_preload.go`（新增） | 启动后台预提取下载目录可执行文件图标（main.go goroutine 触发） |
| `internal/api/server.go` + `middleware.go` | 下载管理/统计路由组改 `adminAuthMiddleware`（新增）；**删除被抢占的 gin `/stats` 死路由（#14）** |
| `internal/api/handlers.go` | 种子保存 `tmp/torrents/` + 清洗；GetTask/PauseTask/DeleteTask 404、Resume/状态冲突 409；**GetStatistics 挂载移除注释（#14）** |
| `protocols/bt/bt.go` | 元数据与写入路径统一清洗；种子 Name 拒绝穿越；finalPath 兜底 |
| `protocols/ftp/ftp.go` | Filename 清洗 + `SafeJoinFile` |
| `protocols/httpx/http.go` | `sanitizeFilename` 加固；Transport 挂 `SafeDialContext`；destPath `SafeJoinFile`；**EOF 丢数据修复（#18）** |
| `internal/core/engine.go` | URL 白名单 + SSRF 校验；`snapshotTask` 深拷贝；Pause 允许 preparing；ctx 派生 + 终态 cancel；Close WaitGroup；终态释放 cancelFunc |
| `internal/core/persistence.go` | SaveTask/SaveProgress 改原子写 |
| `session/session.go` | GetCurrentUser 写锁改读锁；**明文比较恒定时间（#12）；新增 `UpgradeAllPlaintextPasswords`（#13）** |
| `config/config.go` | **SaveConfig 写权限 0644→0600（#13）** |
| `main.go` | **启动批量升级明文密码 + 告警（#13）；配置备份文件权限收紧（#13）** |
| `constants/constants.go` | 版本号 v1.2.0 → v1.3.0 |
| `handlers/ip_stats.go` | **历史日志回填重构（#19/#20）**：ip_stats.json 为空才回填（数据守卫）；(IP+文件) 60s 合并窗口折算；补扫 `.gz`；废弃 `.ip_stats_imported`；新增 `RebuildIPStatsFromLogs`（备份+保留封禁）、`PeriodicSaveIPStats`；保存改原子写；`RebuildIPStatsFromLogs` 返回（备份路径, 保留封禁数, err）；新增 `ipStatsPersistMu` 串行化备份/落盘 |
| `handlers/stats.go` | 周期任务挂接 IP 统计兜底落盘（#19）；热力图 download 点移入 60s 逻辑下载分支 + 新增 `compressHeatmapDownloadPoints` 存量幂等压缩迁移（先排序防乱序误删，#21）；新增 `collectFileStatsFromLogs` + `RebuildFileStatsFromLogs` 按日志重建 stats.json（#22） |
| `handlers/ip_audit.go`（新增） | **界面自检修复（#20/#22）**：`AuditIPStats` 只读对账（当前 vs 日志 60s 真值 / 虚高倍数 / 超期历史提示）；`/api/ip/audit`(GET,admin) + `/api/ip/rebuild`(POST,admin+CSRF)；新增 `AuditFileStats` + `/api/stats/audit` + `/api/stats/rebuild`（双卡右卡，#22） |
| `handlers/ip_admin.go` | IP 管理页新增「🧰 自检修复」按钮/弹窗/JS（对账展示 + 原地二段确认(5s 武装态,无系统弹窗) + 执行重建 + 结果刷新）（#20）；弹窗升级双卡：左 IP 统计 / 右文件统计·热力图（#22） |
| `config/config.go` + `main.go` | 新增配置开关 `server.rebuild_ipstats_on_boot`（#20）；`-rebuild-ipstats` 参数（#19）；新增 `server.rebuild_stats_on_boot` 与 `-rebuild-stats` 参数（#22）；注册 `/api/ip/audit`、`/api/ip/rebuild`、`/api/stats/audit`、`/api/stats/rebuild` 路由（#20/#22） |
| `utils/utils.go` | **客户端 IP 来源加固（#23）**：`normalizeIP` 增加 `net.ParseIP` 格式校验（非法返回空），`GetClientIP` 对非法 XFF / X-Real-IP 头值回退 TCP 对端地址（合法反代场景不回归） |
| `utils/security_test.go` | 新增回归测试 `TestNormalizeIP`（非法值拒绝）与 `TestGetClientIPRejectsSpoofedXFF`（伪造头回退，合法头信任）（#23） |

## 质量验证记录

- `go build ./...` + `go vet ./...`：**零告警**
- 核心业务黑盒：**18/18 PASS**（鉴权/CSRF/业务流/图标公开访问/越权/API Key）
- SSRF：**6/6 PASS**
- 远程下载引擎（真实公网 4 分块 206 + 302）：`completed 100%`，产物 **sha256 与源一致**
- Range 断点续传：206 + Content-Range 精确匹配
- **崩溃恢复（2026-09-06，隔离副本真实强杀）**：`downloading`/`preparing` 遗留自动恢复；29.7MB 下载中强杀 → 重启断点续传 → `completed 100%` → **sha256 与源完全一致**
- **限速下载（2026-09-06）**：SpeedLimit=300KB/s 下载 29.7MB 全程平滑（修复前 31s 撞 Client.Timeout 失败），`completed 100%`
- 遗留建议：后续补 `go test -race ./...`（项目暂无测试文件，建议先写并发用例：同时创建任务 + 订阅 WebSocket 广播）
